CREATE TABLE Users (
    rand_id VARCHAR(255),
    name VARCHAR(255),
    image text,
    mobile text,
    email VARCHAR(255),
    password TEXT,
    verified_at TIMESTAMP WITH TIME ZONE,
    status BOOLEAN,
    cr TIMESTAMP WITH TIME ZONE,   
    up TIMESTAMP WITH TIME ZONE  
);

