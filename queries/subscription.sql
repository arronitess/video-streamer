CREATE TABLE Subscriptions(
    rand_id VARCHAR(255),
    user_rand_id VARCHAR(255),
    channel_id VARCHAR(255),
    status BOOLEAN,
    cr TIMESTAMP WITH TIME ZONE,
    up TIMESTAMP WITH TIME ZONE
);
